"use client";

// STUB — implementado pelo módulo de mídia. Mantém a assinatura do CLAUDE.md.
export function MediaPicker(_props: {
  open: boolean;
  onClose: () => void;
  onSelect: (m: { url: string; alt: string | null }) => void;
  clientId?: string;
}) {
  return null;
}
